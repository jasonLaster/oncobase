diff --git a/apps/app/convex/lib/manifestRevision.ts b/apps/app/convex/lib/manifestRevision.ts
index 050e4f1b..5343cb24 100644
--- a/apps/app/convex/lib/manifestRevision.ts
+++ b/apps/app/convex/lib/manifestRevision.ts
@@ -8 +8 @@ const BUILD_LEASE_MS = 120_000;
-export async function queueManifestBuild(ctx: MutationCtx, siteId: Id<"sites">) {
+export async function queueManifestBuild(ctx: MutationCtx, siteId: Id<"sites">, delayMs = 1000) {
@@ -13 +13 @@ export async function queueManifestBuild(ctx: MutationCtx, siteId: Id<"sites">)
-  await ctx.scheduler.runAfter(1000, internal.manifestBuilder.build, { siteSlug: site.slug });
+  await ctx.scheduler.runAfter(delayMs, internal.manifestBuilder.build, { siteSlug: site.slug });
@@ -18 +18 @@ export async function queueManifestBuild(ctx: MutationCtx, siteId: Id<"sites">)
-export async function invalidateManifest(ctx: MutationCtx, siteId: Id<"sites"> | null) {
+export async function invalidateManifest(ctx: MutationCtx, siteId: Id<"sites"> | null, delayMs = 1000) {
@@ -23 +23 @@ export async function invalidateManifest(ctx: MutationCtx, siteId: Id<"sites"> |
-  await queueManifestBuild(ctx, siteId);
+  await queueManifestBuild(ctx, siteId, delayMs);
diff --git a/apps/app/convex/sites.ts b/apps/app/convex/sites.ts
index 2ceb63c7..32ba7395 100644
--- a/apps/app/convex/sites.ts
+++ b/apps/app/convex/sites.ts
@@ -325 +325 @@ export const finishPublish = mutation({
-    if (owned && site.publishRunChanged !== false) await invalidateManifest(ctx, site._id);
+    if (owned && site.publishRunChanged !== false) await invalidateManifest(ctx, site._id, 0);
@@ -332 +332 @@ export const finishPublish = mutation({
-      if (!reusable) await queueManifestBuild(ctx, site._id);
+      if (!reusable) await queueManifestBuild(ctx, site._id, 0);
