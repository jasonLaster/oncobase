# Optional in local dev. When omitted, Vite proxies /api to http://localhost:3000.
VITE_WIKI_API_ORIGIN=http://localhost:3000

# Optional. Used for links that intentionally hand off to the current Next app
# for backend-owned search, chat, downloads, and sign-in.
VITE_WIKI_APP_ORIGIN=http://localhost:3000
