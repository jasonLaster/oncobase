import { test } from "@playwright/test";

test.describe.skip("Chat", () => {
  test("chat page loads", async () => {});
  test("can send a message and get a response", async () => {});
  test("new chat button in header navigates to chat", async () => {});
  test("mobile bottom sheet shows chat history navigation", async () => {});
});
