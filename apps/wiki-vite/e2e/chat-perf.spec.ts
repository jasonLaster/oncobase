import { test } from "@playwright/test";

test.describe.skip("P0 chat perf", () => {
  test("P0-A composer accepts input", async () => {});
  test("P0-G perf buffer is exposed", async () => {});
  test("first-token and full-completion timings are recorded", async () => {});
});
