# API Shim

This directory contains root-level Vercel API metadata used by the repository deployment shape.

The production application and Vercel function build live in [`apps/app`](../apps/app/README.md). Keep new application routes there unless the root Vercel configuration specifically requires a shim here.
