# API Shim

This directory contains root-level Vercel API metadata used by the repository deployment shape.

The production application and Vercel function build live in [`apps/wiki-vite`](../apps/wiki-vite/README.md). Keep new application routes there unless the root Vercel configuration specifically requires a shim here.
